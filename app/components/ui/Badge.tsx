import type { ReactNode } from 'react';import { cn } from '@/lib/utils';export function Badge({children,className}:{children:ReactNode;className?:string}){return <span className={cn('inline-flex rounded-full border border-brand-accent/40 bg-brand-accent/10 px-4 py-2 font-mono text-xs tracking-[.24em] text-brand-accent',className)}>{children}</span>}
