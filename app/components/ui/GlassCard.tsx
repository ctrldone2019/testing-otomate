import type { ReactNode } from 'react';'use client';import { motion } from 'framer-motion';import { cn } from '@/lib/utils';export function GlassCard({children,className}:{children:ReactNode;className?:string}){return <motion.div whileHover={{y:-8}} className={cn('rounded-[28px] border border-white/10 bg-white/[.045] p-6 backdrop-blur-md transition-shadow hover:shadow-glow',className)}>{children}</motion.div>}
