import type { ReactNode } from 'react';'use client';import { motion } from 'framer-motion';import { cn } from '@/lib/utils';
export function Button({children,variant='primary',className,href}:{children:ReactNode;variant?:'primary'|'ghost';className?:string;href?:string}){const cls=cn('inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-accent',variant==='primary'?'bg-brand-accent text-[#00111f] shadow-glow':'border border-white/10 bg-white/5 text-white hover:bg-white/10',className);return <motion.a href={href} className={cls} whileHover={{scale:1.04}} whileTap={{scale:.98}}>{children}</motion.a>}
