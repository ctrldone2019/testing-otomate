'use client';import { Canvas,useFrame } from '@react-three/fiber';import { useRef } from 'react';import * as THREE from 'three';function Orb(){const m=useRef<THREE.Mesh>(null);useFrame((s)=>{if(m.current){const k=1+Math.sin(s.clock.elapsedTime*1.5)*.08;m.current.scale.setScalar(k)}});return <mesh ref={m}><sphereGeometry args={[1.6,48,48]}/><meshBasicMaterial color="#00a8ff" transparent opacity={0.24}/></mesh>}export default function OrbScene(){return <Canvas dpr={[1,2]} camera={{position:[0,0,5]}}><Orb/></Canvas>}
