'use client';import { useEffect,useRef,useState } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { Check } from 'lucide-react';import { pricing } from '@/lib/constants';import { SectionLabel } from '../ui/SectionLabel';import { Button } from '../ui/Button';import { GlassCard } from '../ui/GlassCard';export function Pricing(){const[yearly,setYearly]=useState(false);const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>gsap.from('.price-card',{y:40,opacity:0,stagger:.12,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}}),ref);return()=>ctx.revert()},[]);return <section id="pricing" ref={ref} className="bg-brand-surface px-5 py-28"><div className="mx-auto max-w-7xl text-center"><SectionLabel>INVESTMENT</SectionLabel><h2 className="font-display text-4xl font-bold md:text-6xl">Choose your growth tier.</h2><p className="mt-5 text-brand-muted">No contracts. No hidden fees. Cancel anytime — but you won't want to.</p><button onClick={()=>setYearly(!yearly)} className="mt-8 rounded-full border border-white/10 bg-black/30 p-1"><span className={!yearly?'rounded-full bg-brand-accent px-5 py-2 text-black':'px-5 py-2'}>Monthly</span><span className={yearly?'rounded-full bg-brand-accent px-5 py-2 text-black':'px-5 py-2'}>Yearly</span></button><div className="mt-12 grid gap-6 lg:grid-cols-3">{pricing.map(p=><GlassCard key={p.name} className={`price-card text-left ${p.popular?'border-brand-accent shadow-glow lg:scale-[1.03]':''}`}>{p.popular&&<p className="mb-4 font-mono text-xs text-brand-accent">MOST POPULAR</p>}<h3 className="font-display text-3xl">{p.name}</h3><p className="mt-1 text-brand-muted">{p.label}</p><p className="mt-6 font-display text-5xl">{yearly?p.yearly:p.monthly}</p><ul className="mt-8 space-y-3">{p.features.map(f=><li className="flex gap-3" key={f}><Check className="text-brand-accent"/>{f}</li>)}</ul><Button href="#cta" className="mt-8 w-full">Get Started</Button></GlassCard>)}</div></div></section>}
