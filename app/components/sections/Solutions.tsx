'use client';import { useEffect,useRef } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { solutions } from '@/lib/constants';import { SectionLabel } from '../ui/SectionLabel';import { GlassCard } from '../ui/GlassCard';export function Solutions(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>gsap.from('.solution-card',{y:40,opacity:0,duration:.8,stagger:.1,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}}),ref);return()=>ctx.revert()},[]);return <section id="solutions" ref={ref} className="px-5 py-28"><div className="mx-auto max-w-7xl"><SectionLabel>WHAT WE INSTALL FOR YOU</SectionLabel><h2 className="font-display text-4xl font-bold md:text-6xl">One system. Every revenue leak — sealed.</h2><p className="mt-5 max-w-3xl text-lg text-brand-muted">Not software you figure out. A done-for-you system we build, install, and run inside your business.</p><div className="mt-12 grid gap-5 md:grid-cols-2">{solutions.map(s=>{const Icon=s.icon;return <GlassCard key={s.title} className="solution-card"><Icon className="h-8 w-8 text-brand-accent"/><h3 className="mt-5 font-display text-2xl font-semibold">{s.title}</h3><p className="mt-3 leading-7 text-brand-muted">{s.body}</p></GlassCard>})}</div></div></section>}
