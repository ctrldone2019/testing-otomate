'use client';import dynamic from 'next/dynamic';import { useEffect,useRef } from 'react';import { ArrowRight,Star } from 'lucide-react';import gsap from 'gsap';import { hero,stats } from '@/lib/constants';import { Badge } from '../ui/Badge';import { Button } from '../ui/Button';import { GradientText } from '../ui/GradientText';import { AnimatedCounter } from '../ui/AnimatedCounter';const HeroCanvas=dynamic(()=>import('../three/HeroCanvas'),{ssr:false,loading:()=> <div className="canvas-placeholder"/>});
export function Hero(){const ref=useRef<HTMLElement>(null);useEffect(()=>{if(!ref.current)return;const ctx=gsap.context(()=>gsap.from('.hero-anim',{y:32,opacity:0,duration:.8,stagger:.08,ease:'power3.out'}),ref);return()=>ctx.revert()},[]);return <section id="hero" ref={ref} className="grain relative min-h-[100dvh] overflow-hidden px-5 pt-28"><div className="mx-auto grid max-w-7xl items-center gap-12 lg:grid-cols-2"><div className="order-2 lg:order-1"><Badge>{hero.badge}</Badge><h1 className="hero-anim mt-8 font-display text-5xl font-bold leading-[.95] md:text-7xl">{hero.title[0]}<br/>to <GradientText>Missed Calls</GradientText>,<br/><GradientText>Cold Leads</GradientText> & <GradientText>Silence</GradientText>.</h1><p className="hero-anim mt-6 max-w-2xl text-lg leading-8 text-brand-muted md:text-xl">{hero.subtitle}</p><div className="hero-anim mt-8 flex flex-wrap gap-4"><Button href="#cta">{hero.primaryCta}</Button><Button href="#how-it-works" variant="ghost">{hero.secondaryCta}<ArrowRight className="ml-2 h-4 w-4"/></Button></div><div className="hero-anim mt-7 flex items-center gap-4 text-sm text-brand-muted"><div className="flex -space-x-3">{['A','I','O'].map(x=><span key={x} className="grid h-9 w-9 place-items-center rounded-full border border-white/20 bg-brand-surface text-xs">{x}</span>)}</div><span>{hero.proof}</span><span className="flex text-brand-accent"><Star/><Star/><Star/><Star/><Star/></span></div><div className="hero-anim mt-8 grid max-w-xl grid-cols-3 rounded-3xl border border-white/10 bg-white/[.03] p-4">{stats.map(s=><div key={s.label}><div className="font-mono text-2xl text-white"><AnimatedCounter value={s.value} suffix={s.suffix}/></div><p className="text-xs text-brand-muted">{s.label}</p></div>)}</div></div><div className="order-1 h-[420px] lg:order-2 lg:h-[620px]"><HeroCanvas/></div></div></section>}
