'use client';import { useEffect,useState } from 'react';import { Button } from '../ui/Button';export function MobileStickyCTA(){const[show,setShow]=useState(false);useEffect(()=>{const on=()=>setShow(scrollY>innerHeight*.75);on();addEventListener('scroll',on);return()=>removeEventListener('scroll',on)},[]);return <div className={`fixed inset-x-0 bottom-0 z-40 border-t border-white/10 bg-black/70 p-3 backdrop-blur md:hidden ${show?'block':'hidden'}`}><Button href="#cta" className="w-full">Book My Free Demo</Button></div>}
