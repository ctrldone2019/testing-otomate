'use client';import { useEffect,useRef } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { Check } from 'lucide-react';import { features,toolCosts } from '@/lib/constants';import { SectionLabel } from '../ui/SectionLabel';export function Features(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>{gsap.from('.feature-row',{y:24,opacity:0,stagger:.04,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}});ScrollTrigger.create({trigger:ref.current,start:'top 70%',onEnter:()=>document.querySelectorAll('.strike').forEach(e=>e.classList.add('is-active')),onLeaveBack:()=>document.querySelectorAll('.strike').forEach(e=>e.classList.remove('is-active'))})},ref);return()=>ctx.revert()},[]);return <section ref={ref} className="px-5 py-28"><div className="mx-auto grid max-w-7xl gap-12 lg:grid-cols-2"><div><SectionLabel>EVERYTHING INCLUDED</SectionLabel><h2 className="font-display text-4xl font-bold md:text-6xl">Replace 12 tools with one done-for-you system.</h2><div className="mt-10 grid gap-3 sm:grid-cols-2">{features.map(f=><div className="feature-row flex gap-3 text-sm" key={f}><Check className="h-5 w-5 shrink-0 text-brand-accent"/>{f}</div>)}</div></div><div className="rounded-[32px] border border-white/10 bg-white/[.04] p-6"><h3 className="font-display text-2xl">Tool stack math</h3><div className="mt-5 space-y-3">{toolCosts.map(([t,c],i)=><div className="flex justify-between border-b border-white/10 pb-3" key={t}><span className={i===9?'font-bold':''}>{t}</span><span className="strike relative font-mono text-brand-muted">{c}</span></div>)}</div><div className="mt-6 rounded-3xl bg-brand-accent/10 p-5"><p className="text-brand-muted">With Otomate: <b className="text-white">starting at $997/mo — done-for-you.</b></p><p className="mt-2 font-display text-3xl text-brand-accent">You save $1,337/month.</p></div></div></div></section>}
