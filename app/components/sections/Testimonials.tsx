'use client';import { useEffect,useRef } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { Star } from 'lucide-react';import { stats,testimonials } from '@/lib/constants';import { AnimatedCounter } from '../ui/AnimatedCounter';import { SectionLabel } from '../ui/SectionLabel';import { GlassCard } from '../ui/GlassCard';export function Testimonials(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>gsap.from('.testimonial',{y:40,opacity:0,stagger:.12,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}}),ref);return()=>ctx.revert()},[]);return <section id="results" ref={ref} className="px-5 py-28"><div className="mx-auto max-w-7xl"><SectionLabel>CLIENT RESULTS</SectionLabel><h2 className="font-display text-4xl font-bold md:text-6xl">1,000+ businesses. Real results.</h2><div className="mt-10 grid gap-4 md:grid-cols-3">{stats.map(s=><div className="rounded-3xl border border-white/10 p-6" key={s.label}><p className="font-display text-4xl text-brand-accent"><AnimatedCounter value={s.value} suffix={s.suffix}/></p><p className="text-brand-muted">{s.label}</p></div>)}</div><div className="mt-12 grid gap-5 md:grid-cols-3">{testimonials.map(t=><GlassCard className="testimonial" key={t[0]}><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-full bg-brand-accent/15 text-brand-accent">{t[0]}</div><div><h3 className="font-semibold">{t[1]}</h3><p className="text-sm text-brand-muted">{t[2]}</p></div></div><div className="mt-5 flex text-brand-accent"><Star/><Star/><Star/><Star/><Star/></div><p className="mt-5 text-brand-muted">“{t[3]}”</p></GlassCard>)}</div></div></section>}
