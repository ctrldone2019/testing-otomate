'use client';import { useEffect,useRef } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { processSteps } from '@/lib/constants';import { SectionLabel } from '../ui/SectionLabel';export function HowItWorks(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>{gsap.from('.step',{y:40,opacity:0,duration:.8,stagger:.15,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}});gsap.to('.timeline-dot',{x:'min(760px,70vw)',duration:1.4,scrollTrigger:{trigger:ref.current,start:'top 75%',toggleActions:'play none none reverse'}})},ref);return()=>ctx.revert()},[]);return <section id="how-it-works" ref={ref} className="bg-brand-surface px-5 py-28"><div className="mx-auto max-w-7xl"><SectionLabel>THE PROCESS</SectionLabel><h2 className="font-display text-4xl font-bold md:text-6xl">From signed to running in 48 hours.</h2><p className="mt-5 text-lg text-brand-muted">We handle everything. You just show up to the onboarding call.</p><div className="relative mt-16 grid gap-8 md:grid-cols-3"><div className="absolute left-10 right-10 top-8 hidden h-px bg-white/10 md:block"><span className="timeline-dot absolute -top-1.5 block h-3 w-3 rounded-full bg-brand-accent shadow-glow"/></div>{processSteps.map((s,i)=><div className="step relative rounded-3xl border border-white/10 bg-black/20 p-7" key={s[0]}><div className="grid h-16 w-16 place-items-center rounded-full border border-brand-accent/40 bg-brand-accent/10 font-mono text-brand-accent">0{i+1}</div><h3 className="mt-6 text-2xl font-semibold">{s[0]}</h3><p className="mt-3 text-brand-muted">{s[1]}</p></div>)}</div></div></section>}
