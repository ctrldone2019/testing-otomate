'use client';import { useEffect,useRef } from 'react';import gsap from 'gsap';import { ScrollTrigger } from 'gsap/ScrollTrigger';import { problems } from '@/lib/constants';import { Button } from '../ui/Button';import { GlassCard } from '../ui/GlassCard';export function ProblemStatement(){const ref=useRef<HTMLElement>(null);useEffect(()=>{gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>gsap.from('.problem-card',{y:40,opacity:0,duration:.8,stagger:.12,scrollTrigger:{trigger:ref.current,start:'top 85%',toggleActions:'play none none reverse'}}),ref);return()=>ctx.revert()},[]);return <section ref={ref} className="px-5 py-28"><div className="mx-auto max-w-7xl text-center"><h2 className="font-display text-4xl font-bold md:text-6xl">Your business is leaking revenue every single day.</h2><p className="mx-auto mt-6 max-w-3xl text-lg text-brand-muted">Every missed call is a customer lost. Every cold lead is money left on the table. Every day without automation is a day your competitors gain ground.</p><div className="mt-12 grid gap-5 md:grid-cols-3">{problems.map(p=><GlassCard key={p[1]} className="problem-card text-left"><div className="text-4xl">{p[0]}</div><h3 className="mt-5 text-xl font-semibold">{p[1]}</h3></GlassCard>)}</div><Button href="#solutions" className="mt-10">Book My Free Demo</Button></div></section>}
