'use client';import { useEffect,useState } from 'react';import { AnimatePresence,motion } from 'framer-motion';import { Menu,X } from 'lucide-react';import { navLinks } from '@/lib/constants';import { Button } from '../ui/Button';
export function Navbar(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const on=()=>setScrolled(scrollY>20);on();addEventListener('scroll',on);return()=>removeEventListener('scroll',on)},[]);return <><header className={`fixed inset-x-0 top-0 z-50 bg-black/40 backdrop-blur-md transition ${scrolled?'border-b border-white/10':''}`}><nav className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5"><a href="#hero" className="font-display text-xl font-bold">OTOMATE<span className="text-brand-accent">.</span></a><div className="hidden gap-8 md:flex">{navLinks.map(l=><motion.a key={l.href} whileHover={{y:-2}} className="text-sm text-brand-muted hover:text-white" href={l.href}>{l.label}</motion.a>)}</div><div className="hidden items-center gap-3 md:flex"><Button href="https://app.otomate.biz" variant="ghost">Login</Button><Button href="#cta">Book My Free Demo</Button></div><button onClick={()=>setOpen(true)} className="md:hidden" aria-label="Open menu"><Menu/></button></nav></header><AnimatePresence>{open&&<motion.div initial={{x:'100%'}} animate={{x:0}} exit={{x:'100%'}} className="fixed inset-0 z-[60] bg-[#070b14] p-6 md:hidden"><button className="float-right" onClick={()=>setOpen(false)} aria-label="Close menu"><X/></button><div className="mt-24 grid gap-8">{navLinks.map((l,i)=><motion.a initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:i*.08}} onClick={()=>setOpen(false)} className="font-display text-3xl" href={l.href} key={l.href}>{l.label}</motion.a>)}<Button href="#cta" className="mt-4">Book My Free Demo</Button></div></motion.div>}</AnimatePresence></>}
